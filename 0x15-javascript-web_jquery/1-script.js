// Add event listner and Wait for the DOM content to be fully loaded
document.addEventListener('DOMContentLoaded', function () {
  // Update the text color of the header element to red (#FF0000)
  $('header').css('color', '#FF0000');
});
